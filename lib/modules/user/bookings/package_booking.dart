import 'package:flutter/material.dart';

class UserPackageBookingScreeen extends StatefulWidget {
  const UserPackageBookingScreeen({super.key});

  @override
  State<UserPackageBookingScreeen> createState() => _UserPackageBookingScreeenState();
}

class _UserPackageBookingScreeenState extends State<UserPackageBookingScreeen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Book your package'),
      ),
      body: Column(

      ),
    );
  }
}
